const gamesContainer=document.getElementById("games"),token=localStorage.getItem("token");token&&fetch("http://localhost:3000/api/games").then(t=>t.json()).then(t=>{t.forEach(t=>{const e=document.createElement("div");e.classList.add("game"),e.innerHTML=`\n                    <img src="${t.image}" alt="${t.title}">\n                    <h3>${t.title}</h3>\n                    <p>${t.description}</p>\n                    <p>Odds: ${t.odds}</p>\n                    <div class="bet-container">\n                        <input type="number" class="bet-amount" placeholder="Enter bet amount">\n                        <button class="bet-button" data-game-id="${t.id}">Place Bet</button>\n                    </div>\n                `,gamesContainer.appendChild(e)}),document.querySelectorAll(".bet-button").forEach(t=>{t.addEventListener("click",t=>{const e=t.target.getAttribute("data-game-id"),n=t.target.previousElementSibling.value;!n||n<=0?alert("Please enter a valid bet amount."):fetch("http://localhost:3000/api/bets",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`},body:JSON.stringify({gameId:e,amount:n})}).then(t=>t.json()).then(t=>{alert(t.message)})})})});